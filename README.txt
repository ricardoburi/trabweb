primeiro de tudo criei a pasta "trabweb2", depois abri o git cmd e localizei o arquivo com os comandandos (cd.. cd.. cd trab2) e então dei o git init para inicializar. na pasta (trabweb2), criei os arquvios index.html e projeto.js, dei um git add ., seguido de um git commit -m "0.1 -arquvio vazio" e deixei comitado a versão. depois adicionei os registros no arquivo index.html e fiz o mesmo processo (git add ., git commit -m "0.2 - arquivo HMTL completo"). realizei o mesmo processo para o arquivo projeto.js (git add ., git commit -m "0.3 - arquivo JS completo"). sempre utilizando do comando "git status" para observar a prontidão do arquivo e o comando "git log" para saber se o commit realmente foi efetivado.